# Sura Essence — Landing Page (V0)

**Purpose**  
Static landing page for Sura Essence (MVP v0). Focused on marketing, lead capture, and validating demand. No backend integrations required for this version.

---

## What’s included
- Next.js app (App Router) with a static landing page
- Tailwind CSS for styling
- Components: `Hero`, `Features`, `Footer`
- App Router API placeholder: `app/api/send-whatsapp/route.ts` (stub; Twilio disabled by default)
- Deployment target: Render (service id: `srv-d4mqpes9c44c7388ip30`)

---

## Quick start (local)

1. Install dependencies
```bash
npm install
